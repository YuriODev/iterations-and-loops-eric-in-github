# class = blueprint for objects
# objects = instances of a class
# attributes = characteristics of a class
# instantiation = creating an instance of an object
# inheritance = inheriting attributes from a parent class to a child class
# composition = a strong relationship between objects, when one is deleted so is the other
# aggregation = a weak realtionship between objects, when one is deleted the other isnt
#encapsulation = wrapping up code into objects


n = input("Enter a number: ")
for i in range(len(n)): print(n[:-i])
		
